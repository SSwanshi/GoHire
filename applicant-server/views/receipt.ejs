<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Successful - GoHire</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body class="receipt-page">  <!-- Added receipt-page class -->
    <!-- Logo and Welcome Message -->
    <header>
        <div class="logo">
            <span class="go">Go</span><span class="hire">Hire</span>
        </div>
        <h1>Payment Successful - GoHire</h1>  <!-- Updated heading -->
        <p>"Your subscription has been activated!"</p>  <!-- Updated message -->
    </header>

    <!-- Receipt Content -->
    <div class="receipt-container">
        <!-- Payment Successful Message -->
        <div class="success-message">
            <div class="success-icon">✓</div>
            Payment Successful!
        </div>

        <!-- Receipt Details -->
        <div class="receipt-details">
            <h3>Transaction Receipt</h3>
            <p><strong>Transaction ID:</strong> <%= transactionId || 'TXN' + Date.now() %></p>
            <p><strong>Payment Method:</strong> <%= paymentDetails.method %></p>
            
            <% if (paymentDetails.method === 'Credit/Debit Card') { %>
                <p><strong>Card Number:</strong> **** **** **** <%= paymentDetails.cardNumber %></p>
                <p><strong>Cardholder Name:</strong> <%= paymentDetails.cardholderName %></p>
            <% } else if (paymentDetails.method === 'Net Banking') { %>
                <p><strong>Bank:</strong> <%= paymentDetails.bankName %></p>
                <p><strong>User ID:</strong> <%= paymentDetails.bankUserId %></p>
            <% } else if (paymentDetails.method === 'UPI') { %>
                <p><strong>UPI ID:</strong> <%= paymentDetails.upiId %></p>
                <p><strong>UPI App:</strong> <%= paymentDetails.upiApp %></p>
            <% } %>
            
            <p><strong>Subscription Plan:</strong> <%= subscriptionPlan || 'Premium Plan' %></p>
            <p><strong>Amount Paid:</strong> ₹<%= amount %></p>
            <p><strong>Transaction Date:</strong> <%= new Date().toLocaleString() %></p>
            <p><strong>Status:</strong> <span style="color: #28a745; font-weight: bold;">COMPLETED</span></p>
        </div>

        <!-- Thank You Message -->
        <p class="thank-you">Thank you for your payment! Your subscription is now active.</p>

        <!-- Return Home Link -->
        <a href="/" class="return-home">Return to Homepage</a>
    </div>
</body>
</html>